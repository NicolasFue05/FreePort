{
  "name": "freeport",
  "version": "1.0.0",
  "description": "Module to check if a port is available or not.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "author": "Nicolas Fuentes - @NicolasFue05",
  "license": "ISC",
  "dependencies": {
    "picocolors": "1.1.1",
    "standard": "^17.1.2"
  },
  "eslintConfig": {
    "extends": "standard",
    "rules": {
      "semi": [
        "error",
        "never"
      ],
      "quotes": [
        "error",
        "single"
      ]
    }
  }
}
